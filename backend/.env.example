# Railway 환경 변수 설정 가이드
# Railway 대시보드에서 Variables 탭에 다음 값들을 입력하세요

# 서버 설정
PORT=5000
NODE_ENV=production

# MySQL 데이터베이스 (Railway MySQL 사용 시 자동 설정됨)
# Railway에서 MySQL 플러그인 추가하면 다음 변수들이 자동으로 생성됩니다:
# MYSQLHOST
# MYSQLPORT
# MYSQLUSER
# MYSQLPASSWORD
# MYSQLDATABASE

# 아래 변수는 Railway MySQL 플러그인의 값을 사용하도록 설정하세요
DB_HOST=${MYSQLHOST}
DB_PORT=${MYSQLPORT}
DB_USER=${MYSQLUSER}
DB_PASSWORD=${MYSQLPASSWORD}
DB_NAME=${MYSQLDATABASE}

# JWT 시크릿 키 (반드시 변경하세요!)
JWT_SECRET=your-production-secret-key-change-this-to-random-string-2024
JWT_EXPIRE=30m

# 세션 설정
SESSION_TIMEOUT=1800000

# CORS 설정 (프론트엔드 URL)
FRONTEND_URL=https://your-frontend-url.vercel.app
